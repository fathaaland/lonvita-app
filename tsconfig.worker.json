{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./worker/dist",
    "noEmit": false,
    "module": "esnext",
    "moduleResolution": "bundler"
  },
  "// include": "Was a hand-picked list of the few files the worker touched. The cleanup job pulls in @payload-config, and that reaches the whole collection tree, so the worker now type-checks against all of src.",
  "include": ["src/**/*.ts", "worker/src/**/*.ts"]
}
